#/usr/bin/python

class BaseModuleObject(object):
    """ Describes the interface that module models are expected to support

    This class should never be instantiated directly, but should be used as a class base for
    the ojects being wrapped by the external process that this module implements """
    
    pass
